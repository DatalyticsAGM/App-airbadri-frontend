/**
 * Booking Detail Page (Static Route)
 * 
 * Página de detalle de reserva usando ruta estática con query params
 * Necesario para funcionar con output: 'export'
 */

'use client';

import { BookingDetailPageClient } from '../[id]/booking-detail-client';

export default function BookingDetailPage() {
  return <BookingDetailPageClient />;
}

